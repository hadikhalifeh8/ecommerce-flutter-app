import 'package:flutter/material.dart';

class Item_s extends StatelessWidget {
  const Item_s({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("product"),);
  }
}